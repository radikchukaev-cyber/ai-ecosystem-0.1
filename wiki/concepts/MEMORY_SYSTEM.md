---
title: "MEMORY_SYSTEM"
date: "2026-06-05"
agent: "SYSTEM"
status: "active"
---
# 📜 СИСТЕМА ПАМЯТИ И MEMORY.MD (MEMORY SYSTEM)

> [!WARNING]
> **ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ**
> Файл `MEMORY.md` — это сердце системы. Ничто не существует, если оно не записано в память.

## 1. РОЛЬ MEMORY.MD
`MEMORY.md` (находящийся в корне `wiki/`) является главным шлюзом и точкой синхронизации для всех агентов. 
- Это **Рабочая Память** всей системы.
- RAMS читает этот файл перед каждым циклом планирования.
- Любой агент, начиная задачу, обязан записать статус в `MEMORY.md`.

## 2. ЖИЗНЕННЫЙ ЦИКЛ ЗАДАЧИ В ПАМЯТИ
1. **Инициация:** Шеф ставит задачу. RAMS записывает её в `MEMORY.md` как `[TODO]`.
2. **Взятие в работу:** Агент (например, ВУЛКАН) видит задачу, меняет статус на `[IN_PROGRESS]` и указывает ссылку на свой локальный лог в `wiki/entities/Agents/Vulcan/memory/`.
3. **Исполнение:** Все промежуточные мысли и чек-листы пишутся в локальную папку агента.
4. **Завершение:** Агент возвращается в `MEMORY.md` и пишет `[DONE]`. Оставляет ссылку на результат.

## 3. ПРАВИЛА ВЕДЕНИЯ ЛОГОВ (LOGGING)
- **Метка времени:** Каждая запись должна начинаться с времени `[HH:MM]`.
- **Идентификация:** Агент должен указывать свое имя `ВУЛКАН: Начинаю писать код...`.
- **Без воды:** Только суть. Развернутые логи лежат в `wiki/synthesis/`.

## 4. СИНХРОНИЗАЦИЯ С ДОЛГОСРОЧНОЙ ПАМЯТЬЮ
Когда `MEMORY.md` переполняется (в конце дня):
1. **МНЕМОЗИНА** переносит успешные завершенные задачи в `wiki/synthesis/History/`.
---
title: "MEMORY_SYSTEM"
date: "2026-06-05"
agent: "SYSTEM"
status: "active"
---
# 📜 СИСТЕМА ПАМЯТИ И MEMORY.MD (MEMORY SYSTEM)

> [!WARNING]
> **ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ**
> Файл `MEMORY.md` — это сердце системы. Ничто не существует, если оно не записано в память.

## 1. РОЛЬ MEMORY.MD
`MEMORY.md` (находящийся в корне `wiki/`) является главным шлюзом и точкой синхронизации для всех агентов. 
- Это **Рабочая Память** всей системы.
- RAMS читает этот файл перед каждым циклом планирования.
- Любой агент, начиная задачу, обязан записать статус в `MEMORY.md`.

## 2. ЖИЗНЕННЫЙ ЦИКЛ ЗАДАЧИ В ПАМЯТИ
1. **Инициация:** Шеф ставит задачу. RAMS записывает её в `MEMORY.md` как `[TODO]`.
2. **Взятие в работу:** Агент (например, ВУЛКАН) видит задачу, меняет статус на `[IN_PROGRESS]` и указывает ссылку на свой локальный лог в `wiki/entities/Agents/Vulcan/memory/`.
3. **Исполнение:** Все промежуточные мысли и чек-листы пишутся в локальную папку агента.
4. **Завершение:** Агент возвращается в `MEMORY.md` и пишет `[DONE]`. Оставляет ссылку на результат.

## 3. ПРАВИЛА ВЕДЕНИЯ ЛОГОВ (LOGGING)
- **Метка времени:** Каждая запись должна начинаться с времени `[HH:MM]`.
- **Идентификация:** Агент должен указывать свое имя `ВУЛКАН: Начинаю писать код...`.
- **Без воды:** Только суть. Развернутые логи лежат в `wiki/synthesis/`.

## 4. СИНХРОНИЗАЦИЯ С ДОЛГОСРОЧНОЙ ПАМЯТЬЮ
Когда `MEMORY.md` переполняется (в конце дня):
1. **МНЕМОЗИНА** переносит успешные завершенные задачи в `wiki/synthesis/History/`.
2. **СОКРАТ** анализирует логи ошибок и делает "Learning Notes".
3. **КАСПЕР** очищает `MEMORY.md`, готовя его к новому дню.

---
*Очищено и обновлено хранителем Mnemosyne.*

***
**Отметки:** [[wiki/MEMORY.md|#memory]] [[wiki/concepts/CONSTITUTION.md|#rules]] [[wiki/synthesis/SYNTHESIS_INDEX.md|#learning]]
