# Журнал интеграции Telegram
Статус: Активен.
Принцип: Все данные из Telegram-сессий напрямую попадают в основную память (MEMORY.md).
Протокол: Агент RAMS принудительно выполняет синхронизацию (git pull + git push) до и после каждой сессии, чтобы поддерживать актуальность «Вечного Разума».